server {
    listen 5000;
    server_name _;

    root /app;
    index index.html;

    location = / {
        try_files /index.html =404;
    }

    location / {
        try_files $uri $uri/ $uri/index.html =404;
    }
}
